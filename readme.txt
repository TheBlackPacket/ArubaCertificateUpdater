To run this, call python3 ./CertUpdater.py config.yaml

config.yaml needs to be the path to a config file. Look through the provided example yaml document to get an idea on how to configure it.

Goodluck.